# ~/.custom_rc — portable shell config
# Source this from any system's .bashrc: [ -f ~/.custom_rc ] && . ~/.custom_rc

# ==============================================================================
# HISTORY & SHELL OPTIONS
# ==============================================================================
HISTSIZE=10000
HISTFILESIZE=20000
HISTCONTROL=ignoreboth:erasedups
shopt -s histappend checkwinsize cdspell autocd 2>/dev/null

# ==============================================================================
# PATH
# ==============================================================================
export PATH="$HOME/.local/bin:$HOME/.npm-global/bin:$PATH"
export EDITOR=vim

# ==============================================================================
# ENVIRONMENT & GENERAL SETTINGS
# ==============================================================================
export EDITOR=vim

# Core Shell Completions
if ! shopt -oq posix 2>/dev/null; then
    [ -f /usr/share/bash-completion/bash_completion ] && . /usr/share/bash-completion/bash_completion
    [ -f /etc/bash_completion ] && . /etc/bash_completion
fi

# Disable terminal output freeze to make Ctrl+S work in Tmux Resurrect
stty -ixon

# ==============================================================================
# ALIASES & TOOLS
# ==============================================================================
alias ls='ls --color=auto'
alias ll='ls -alF'
alias la='ls -A'
alias grep='grep --color=auto'
alias k='kubectl'
alias kc='kubectl create'
alias gpush='git push origin HEAD:refs/for/master'
alias vpn_login='globalprotect connect --portal ://gpcloudservice.com'
export dry='--dry-run=client -o yaml'

# Tool Autocompletions
command -v kubectl &>/dev/null && source <(kubectl completion bash 2>/dev/null)
[ -f ~/.fzf.bash ] && . ~/.fzf.bash


# Modern Terminal Prompt Setup
eval "$(starship init bash)"
# External Profiles
[ -f ~/.secret_rc ] && . ~/.secret_rc
[ -f ~/.work_ssh_rc ] && . ~/.work_ssh_rc

# Fast Directory Jumping, keep yhis on last line
command -v zoxide &>/dev/null && eval "$(zoxide init bash)"
